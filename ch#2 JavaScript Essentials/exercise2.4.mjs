/* Create variables for three numbers: a, b, and c. Update these
   variables with the following actions using the assignment operators */

let a = 10; //here we are hardcode the value of all the variables
let b = 8; //we can also get value of a,b & c by prompt.
let c = 24;

//Add b to a

a = a + b; //we can also write a +=b;
console.log(a);
//Divide a by c
b = a / c;
console.log(b);
//Replace the value of c with the modulus of c and b

c = c % b;
console.log(c);
